# The ManFuzzer

Given a binary path this file produces test cases to that binary
based on the flags found in -h,-H,--help and the man page. There is also the
option to execute those test cases.

Originally created by Peter Chapman in 2012, it was recently modified for a [large-scale
experiment on vulnerability prediction](TODO).
